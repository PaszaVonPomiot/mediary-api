name: mediary
services:
  api:
    container_name: fastapi
    build:
      context: .
      dockerfile: dockerfiles/api/Dockerfile
      args:
        APP_NAME: ${APP_NAME}
    ports:
      - 80:8000
    env_file:
      - .env
    environment:
      APP_ENV: ${APP_ENV}
    depends_on:
      - db
    volumes:
      - ./mediary/app:/mediary/app
      - ./mediary/settings:/mediary/settings
      - ./mediary/alembic:/home/alembic
  db:
    container_name: postgres
    build:
      dockerfile: dockerfiles/db/Dockerfile.alpine
    ports:
      - 5432:5432
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - ./db/data:/var/lib/postgresql/dataservices
